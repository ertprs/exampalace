import React from 'react';

const ButtonRail = ({ children }) => {
  return (
    <div
      style={{
          padding: '8px'
      }}
    >
      {children}
    </div>
  );
};

export default ButtonRail;
